# Database
# Path to SQLite database file (defaults to ./city.db)
# DATABASE_URL=./city.db
# DB_PATH=./city.db

# Logging
# Log level: trace, debug, info, warn, error, fatal (defaults to debug in dev, info in production)
# LOG_LEVEL=info

# Sentry (optional — error tracking is disabled when DSN is not set)
# NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# SENTRY_AUTH_TOKEN=sntrys_...  (only needed in CI for source map uploads)

# Electron code signing (only needed for distribution builds)
# CSC_LINK=path/to/certificate.p12
# CSC_KEY_PASSWORD=certificate-password
# APPLE_ID=your@apple.id
# APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
# APPLE_TEAM_ID=XXXXXXXXXX
